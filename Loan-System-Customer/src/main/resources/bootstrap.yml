spring:
  application:
    name: loan-customer-system
  cloud:
    config:
      name: customer-system
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
    