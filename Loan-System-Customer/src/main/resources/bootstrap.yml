spring:
  application:
    name: CustoemrSystem
  cloud:
    config:
      name: customer-system
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka 