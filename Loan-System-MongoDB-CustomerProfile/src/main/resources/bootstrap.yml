spring:
  application:
    name: customer-profile
  cloud:
    config:
      name: customer-profile
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
      #uri: http://localhost:8888/
      